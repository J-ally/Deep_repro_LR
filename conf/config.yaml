data:
  path: data/mnist_flat.pkl

model:
  solver: lbfgs
  l1_ratio: 0
  C: 1.0

training:
  max_iter: 1000

exp_name: logistic regression
checkpoints_dir: checkpoints/
mlflow_db: sqlite:///mlflow/mlflow.db